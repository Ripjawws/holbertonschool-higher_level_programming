#!/usr/bin/node
/*
Prints 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop
*/
const arr = ['C is fun', 'Python is cool', 'JavaScript is awesome'];
for (let i = 0; i <= 2; i++) {
  console.log(arr[i]);
}
